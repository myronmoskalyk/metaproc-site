---
// Real UI screenshot, captured from the live containerized app (Stage 4 → wired in Stage 7).
// Marked data-proof-asset so tooling can find every proof slot.
// Emits <picture> with AVIF + WebP sources, srcset, and explicit dimensions (no CLS).
import { Picture } from 'astro:assets';
import type { ImageMetadata } from 'astro';

interface Props {
	image: ImageMetadata;
	alt: string;
	caption: string;
	widths?: number[];
	sizes?: string;
	loading?: 'lazy' | 'eager';
}
const {
	image,
	alt,
	caption,
	widths = [480, 800, 1200, 1600],
	sizes = '(min-width: 64rem) 22rem, (min-width: 40rem) 45vw, 92vw',
	loading = 'lazy',
} = Astro.props;
---
<figure class="mp-proof" data-proof-asset>
	<Picture
		src={image}
		alt={alt}
		formats={['avif', 'webp']}
		fallbackFormat="webp"
		widths={widths}
		sizes={sizes}
		loading={loading}
		decoding="async"
	/>
	<figcaption>{caption}</figcaption>
</figure>

<style>
	.mp-proof {
		margin: 0;
	}
	.mp-proof :global(img) {
		display: block;
		width: 100%;
		height: auto;
		border: 1px solid var(--mp-edge);
		border-radius: var(--mp-radius-lg);
		background: var(--mp-panel);
		box-shadow: var(--mp-shadow-panel);
		transition: transform .18s var(--mp-ease), box-shadow .22s ease;
	}
	.mp-proof:hover :global(img) {
		transform: translateY(-2px);
		box-shadow: var(--mp-shadow-panel), 0 0 30px -14px var(--mp-glow);
	}
	.mp-proof figcaption {
		margin-top: 0.6rem;
		font-size: 0.9rem;
		color: var(--mp-muted);
	}
</style>
